#include<bits/stdc++.h>
using namespace std;
int fib(int n)
{
    if(n==0||n==1)
    {
        return n;
    }
    int sm1=fib(n-1);
    int sm2=fib(n-2);
    return sm1+sm2;
    
}
int main()
{
    int n;
    cin>>n;
    cout<<fib(n)<<endl;
    
}
